#include "main.h"
#include <string.h>

/**
 * _strlen - function to return the length of a string
 * @s: string
 * Return: length
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
