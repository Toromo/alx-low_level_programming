#include "main.h"
#include <string.h>

/**
 * _strlen - function to return the length of a string
 * @s: input
 * Return: always 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
